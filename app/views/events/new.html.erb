<%= render 'shared/navbar' %>
<%= render 'shared/menubar' %>

<h3>Create your own event!</h3>

<div class="container p-3">
  <%= simple_form_for(@event) do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :number_of_participants%>
    <%= f.input :languages, collection: ["english", "french", "german", "italian", "portuguese", "other"]%>
    <%= f.input :address %>
    <%= f.input :date %>
    <% EventType.all.each do |event_type| %>
      <img class="image-event" src="<%= event_type.image_url %>" data-event-type-id="<%=event_type.id%>">
    <% end %>
    <div id="event-radios" class="d-none">
       <%= f.collection_radio_buttons :event_type_id, EventType.all,:id, :name %>
    </div>
    <%= f.submit class: 'btn float-right w-100 mb-5' %>
  <% end %>

</div>


<script></script>
